services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: flinkbase
      POSTGRES_USER: flinkbase
      POSTGRES_PASSWORD: flinkbase
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
